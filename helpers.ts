import {
    formatDate,
    formatTime,
    formatName,
    formatScoreType,
    getTeamName,
    getTeamBackground,
    groupEvents,
    getScheduleHours,
    getEventScheduleStyles,
} from './util';
import { getAvatarUrl } from './discordApi';

export default {
    formatDate,
    formatTime,
    formatName,
    formatScoreType,
    getTeamName,
    getTeamBackground,
    getAvatarUrl,
    groupEvents,
    getScheduleHours,
    getEventScheduleStyles,
}

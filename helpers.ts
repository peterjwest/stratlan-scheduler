import {
    formatDate,
    formatTime,
    formatName,
    formatScoreType,
    getTeamName,
    getTeamBackground,
    groupEvents,
    getScheduleHours,
    getEventScheduleStyles,
    isAdmin,
} from './util';
import { getAvatarUrl } from './discordApi';

export default {
    formatDate,
    formatTime,
    formatName,
    formatScoreType,
    getTeamName,
    getTeamBackground,
    getAvatarUrl,
    groupEvents,
    getScheduleHours,
    getEventScheduleStyles,
    isAdmin,
}
